- name: configure redis server
  become: yes
  hosts: redis
  tasks:
  - name: enable redis 7
    ansible.builtin.command: dnf module enable redis:7 -y

  - name: inatall redis
    ansible.builtin.dnf:
      name: redis
      state: present